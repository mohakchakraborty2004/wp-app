import { SafeAreaView, Text, View } from "react-native";

export default function AccountInfo() {
    return <SafeAreaView>
        <Text>
            account Info
        </Text>
    </SafeAreaView>
}